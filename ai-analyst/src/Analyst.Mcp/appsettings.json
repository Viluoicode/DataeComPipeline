{
  "// ConnectionStrings": "No secret in source control. Password is injected at runtime via the ConnectionStrings__Analyst env var (Docker/prod) or user-secrets (local dev). analyst_ro is read-only on the Gold schema.",
  "ConnectionStrings": {
    "Analyst": "Server=localhost,11433;Database=AnalystDB;User Id=analyst_ro;Password=;TrustServerCertificate=True;Encrypt=False"
  },
  "Analyst": {
    "SchemaConfigPath": "config/schema.fnb.json",
    "Provider": "Offline",
    "Azure": { "Endpoint": "", "DeploymentName": "", "ApiKey": "" }
  }
}
