# Secrets — never commit real credentials
.env

# Python virtual env
.venv/
venv/
__pycache__/
*.pyc

# Local warehouse + landing data (regenerated by the extract job)
*.duckdb
*.duckdb.wal
data/raw/*.parquet

# dbt artifacts
target/
dbt_packages/
logs/
